version: '2'

services:
  be:
    env_file: .env
    image: 'node:16.13.0'
    user: 'node'
    working_dir: /home/node/app/dist
    environment:
      NODE_ENV: production
      HOST: '${BE_HOST}'
      PORT: '${BE_PORT}'
      MONGO_URL: 'mongodb://mongoDB:27017'
      MONGO_DATABASE: '${MONGO_DATABASE-tm-ks}'
    volumes:
      - ./be:/home/node/app
    ports:
      - '8080:8080'
    command: 'npm run build:start'
  mongoDB:
    image: mongo:latest
    ports:
      - "27017:27017"
